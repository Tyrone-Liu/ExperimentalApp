<?xml version ="1.0" encoding ="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="@string/preference_category_title_capture_and_storage">

        <SwitchPreference
            android:defaultValue="true"
            android:key="preference_key_capture_raw"
            android:summary="@string/preference_summary_capture_raw"
            android:title="@string/preference_title_capture_raw" />

    </PreferenceCategory>

    <!--
    TODO list:
    max preview resolution
    store file name
    store file path
    max preview exposure time (prevent shutter lag)
    -->

</PreferenceScreen>
